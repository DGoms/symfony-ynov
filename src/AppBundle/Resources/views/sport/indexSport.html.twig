<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
</head>
<body>
    <div>
        <a href="{{ path('home') }}">HOME</a><br>
        <br>
        <br>
        {% for sport in sports %}
            <a href="{{ path('sport_show', {'id': sport.id}) }}">{{ sport.libelle }}</a> <br>
        {% endfor %}
    </div>
</body>
</html>